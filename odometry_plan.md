# 보류 작업 — 엔코더 오도메트리 · 좌표 실수화

> **이 문서를 꺼내는 시점**: 엔코더 하드웨어가 완성됐을 때.
> 2026-08-26 기준으로 하드웨어가 미완성이라 착수하지 않기로 한 작업을,
> 나중에 그대로 꺼내 쓸 수 있게 정리해 둔 것이다.
> 지금 진행 중인 작업(경로 스무딩 등)은 이 문서와 무관하게 돌아간다.

---

## 0. 현재 상태 (2026-08-26)

| | 상태 |
|---|---|
| 엔코더 하드웨어 | ❌ **미완성** (사용자 확인). 구현 방법도 미정 |
| 엔코더 핀 배정 | ✅ 되어 있음 — R: 18(A)/23(B), L: 34(A)/35(B) |
| 엔코더 펌웨어 | ❌ 없음. `rescue_sensor_node.ino` 는 `PIN_ENC_*` 를 선언만 하고 안 씀 |
| 자이로(헤딩) | ✅ 있음 — MPU6050, 스트림의 `yaw` 필드로 이미 들어옴 |
| 거리 측정 수단 | ❌ 없음 → 그래서 지금은 "거리를 시간으로 번역"해서 명령한다 |

핀은 좌우 각각 A/B 2채널이라 **직교(quadrature) 방식**이다. 완성되면
방향 판별(전진/후진)까지 된다. 34/35 는 ESP32 **입력 전용** 핀이라
내부 풀업이 없다 — 엔코더 모듈이 라인을 능동 구동하는지 확인할 것.

### ★선행 확인 — 엔코더와 무관하게 먼저 풀어야 할 것

`motor_commander.py` 는 `CMD_PORT`(8890)로 `forward`/`turn_left`/
`turn_right` 를 보내는데, 같은 8890 을 여는 `rescue_sensor_node.ino` 의
`handleCommand()` 는 `sweep`/`hold`/`aim`/`scan`/`ping` 만 안다 —
**모터 명령이 조용히 무시되고 있다.** ESP32 가 한 대인지 두 대인지도
미확인(`robot_config.py` 엔 `ESP32_IP` 가 하나뿐). 이게 정리되지 않으면
엔코더를 붙여도 이동 명령 자체가 안 나간다.

---

## 1. 착수 순서

### 1-1. ESP32 — 인터럽트 기반 틱 카운터

A/B 두 채널의 엣지에서 인터럽트를 걸고, 두 채널의 위상차로 방향을
판정해 카운터를 증감시킨다. 카운터는 `volatile long` 으로 두고 ISR 은
최대한 짧게(카운터 증감만) 유지할 것.

### 1-2. `MM_PER_TICK` 실측

바퀴 지름과 엔코더 해상도로 계산은 되지만, 타이어 눌림·기어 백래시
때문에 **실측이 정답**이다.

1. 로봇을 직선으로 충분히 길게(예: 2m) 굴린다 — 길수록 오차 비율이 준다
2. 실제 이동거리를 자로 재고, 그 구간의 좌우 틱 수를 읽는다
3. `MM_PER_TICK = 실제거리 / 평균틱수`
4. 최소 5회 반복해 평균. 편차가 크면 바닥재/하중 조건을 고정하고 재측정

★ 이 값은 **상수가 아니다** — 타이어 마모, 하중, 바닥재에 따라 변한다.
한 번 재고 끝내지 말 것.

### 1-3. 좌우 바퀴 특성 차이 보정

같은 PWM 에 좌우 속도가 다른 게 정상이다. 직진 명령을 주고 좌우 틱이
얼마나 벌어지는지 재서 보정 계수를 잡는다. 이게 안 맞으면 로봇이
직진 명령에도 호를 그린다.

### 1-4. 스트림에 필드 추가 — `.ino`

`sendStream()` 에 좌우 누적 틱을 넣는다. 현재 스키마:

```json
{"type":"stream","servo":..,"dist":..,"st":..,"valid":..,"t":..,"yaw":..,"temp":..}
```

여기에 `"enc_l"`, `"enc_r"` 를 추가. 버퍼가 `char buf[224]` 라 크기
확인 필요.

### 1-5. 파이썬 수신 — ★현재 페이로드를 버리고 있다

`socket_receiver.py` 의 stream 분기는 카운터만 올리고 필드를 하나도
꺼내지 않는다.

```python
elif t == "stream":
    self.stat["stream"] += 1      # 페이로드 폐기
```

`tof_commander.route_message()` 도 `dist/servo/st/gas/yaw` 만 꺼낸다
(CLAUDE.md §"온도가 파이썬으로 안 넘어간다" 참고 — `temp` 도 같은
이유로 안 넘어오는 중). 엔코더 필드를 실어도 지금 구조면 버려진다.

### 1-6. 차동구동 오도메트리 공식

```
d_L = (좌틱 변화량) × MM_PER_TICK
d_R = (우틱 변화량) × MM_PER_TICK

전진량  d  = (d_L + d_R) / 2
회전각 Δθ  = (d_R - d_L) / 축간거리(WHEEL_BASE_MM)

x += d × cos(θ)
y += d × sin(θ)
θ += Δθ
```

`WHEEL_BASE_MM` 도 실측 필요. 헤딩은 자이로(`yaw`)와 이 식이 둘 다
값을 주므로 **어느 쪽을 신뢰할지 정해야 한다** — 보통 자이로가 짧은
시간엔 정확하고 엔코더는 슬립에 취약하므로, 자이로를 주로 쓰고
엔코더는 교차검증에 쓰는 편이 낫다.

### 1-7. 명령 프로토콜 전환

엔코더가 붙으면 "거리를 시간으로 번역"할 필요가 사라진다.

```
지금  : {"cmd":"forward", "speed":100, "duration_ms":520}   ← 추정 속도로 나눈 값
이후  : {"cmd":"forward", "distance_mm":130}                 ← ESP32 가 틱 세며 도달 확인
```

이때 `motor_commander.move_forward()` 내부만 바꾸면 되도록, **호출부는
지금 미리 mm 단위로** 말하게 해 둔다(진행 중인 "명령 계층 정리" 작업).

---

## 2. 좌표 실수화 — 엔코더 이후에 의미가 생긴다

### 2-1. 왜 지금 하지 않나

현재 시뮬 구조에서 `robot_x` 를 float 로 바꿔도 **기능적으로 바뀌는 게
거의 없다**:

| | 영향 | 이유 |
|---|---|---|
| 센싱 | 없음 | `sweep_while_moving` 이 이미 내부에서 보간해 float 로 레이캐스팅 |
| 계획 | 없음 | 스텝 경계에서만 일어나고 그 순간 로봇은 셀 위에 있음 |
| 표시 | 없음 | `viz.progress()` 가 이동 **완료 후 1회만** 호출됨 |

즉 "좌표 실수화" 의 실체는 변수 타입이 아니라 **"이동 중간 프레임을
`viz` 에 밀어넣기"** 다. 엔코더가 생겨서 "실제 위치가 셀 사이" 인
상황이 실재하기 전까지는 미관 개선에 가깝다.

### 2-2. 착수 시 — 깨지는 곳은 딱 2곳

전수 조사 결과 대부분은 이미 float 안전하다(`has_line_of_sight`,
`find_nearest_unknown`, `find_confirm_point`, `real_scan`, `record_hit`,
`bump_cells` 전부 내부에서 `int(round())` 하거나 순수 산술만 쓴다).

| 위치 | 증상 | 고칠 방법 |
|---|---|---|
| `bfs_from()` 호출부 (`path_planner_sim2.py:1740`, `:1895`) | `TypeError` — `si = sy*GRID_SIZE+sx` 가 배열 인덱스 | 호출부에서 `int()` |
| `(robot_x, robot_y) == home` (`:1885`) | ★**조용히** 영원히 False → 복귀 완료 판정 불가 | `math.hypot(...) < 0.5` 거리 판정 |

두 번째가 위험하다 — 에러 없이 로봇이 집 앞에서 계속 맴돈다.

### 2-3. 렌더링 보간의 제약 — GIL

`sim_bridge.DashViz.progress()` 주석에 적혀 있듯, 시뮬 스레드가 무거운
계산으로 GIL 을 붙잡으면 Qt 메인 스레드가 못 그려서 렉이 걸린다. 그래서
`progress()` 는 **호출 빈도를 일부러 제한**하고 있다. 중간 프레임을
늘리면 이 문제를 다시 건드리게 되므로, 보간 프레임 수를 적당히 제한할 것.

---

## 3. ★엔코더로도 안 풀리는 것 — 반드시 인지할 것

엔코더는 "열린 루프 → 닫힌 루프" 로 가는 큰 도약이지만 **절대 위치를
주지는 않는다.** 실측을 마쳐도 아래는 그대로 남는다.

1. **슬립** — 바퀴가 헛돌면 엔코더는 "갔다" 고 센다. 원리적으로 감지
   불가. 재난 현장(잔해·경사·먼지)에서 특히 취약하다
2. **누적 드리프트** — 오차는 계속 쌓인다. 엔코더는 드리프트를 **늦출
   뿐 없애지 못한다**
3. **자이로 바이어스 드리프트** — MPU6050 을 적분해 헤딩을 쓰면 시간에
   따라 틀어진다. 각도 오차가 위치 오차의 주범이다
4. **`MM_PER_TICK` 변동** — 위 1-2 참고

### 규모감

무대는 `GRID_SIZE=60 × CELL_SIZE=0.05` = **3m × 3m**. 탐사 미션이면 총
주행거리가 10~20m 는 나온다. 휠 오도메트리는 대략 주행거리의 수 %
수준으로 드리프트하는 게 보통이므로(경험적 범위, 실측 전엔 추정):

```
주행 15m × 3% ≈ 45cm 오차 = 9칸
```

3m 무대에서 9칸이면 무시 못 한다. **엔코더 실측 하나로 "확실하게" 가
되지는 않는다.**

---

## 4. 세 번째 조각 — ToF 기반 절대 참조

드리프트를 끊으려면 절대 참조가 필요하다.

```
엔코더  → 상대 이동량 (부드럽지만 드리프트)
자이로  → 헤딩        (이미 있음, 역시 드리프트)
+ ToF·지도 대조 → 절대 참조 (가끔이지만 드리프트 리셋)   ★이게 있어야 "확실"
```

**추가 하드웨어 없이 가능하다** — ToF 도 격자 지도도 이미 있다.
초보적으로는 "지도상 벽까지 1200mm 나와야 하는데 실측 900mm → 300mm 더
왔구나" 로 보정하는 것부터 시작할 수 있다(스캔 매칭의 단순형).

---

## 5. 관련 문서

- `CLAUDE.md` §"이동 제어 / 경로 실행 (2026-08-26)" — `duration_ms`
  고정 버그, 시뮬/실기 속도 상수 불일치, 소수 표현 현황
- `CLAUDE.md` §"ESP32 핀 배치 (2026-08-26 확정)" — 핀 단일 출처
